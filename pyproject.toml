[build-system]
requires = [ "setuptools", "wheel",]
build-backend = "setuptools.build_meta"

[project]
name = "vms"
version = "0.1.12"
description = "funciones cool que uso en el dia a dia y esas cosas"
dependencies = [ "pyarrow", "polars", "xlwings", "pandas", "plyer",]
