[build-system]
requires = [ "setuptools", "wheel",]
build-backend = "setuptools.build_meta"

[project]
name = "vms"
version = "0.1.7"
description = "Mi paquete personalizado de funciones"
dependencies = [ "pyarrow", "polars", "xlwings", "pandas", "plyer",]
